import 'package:flutter/material.dart';

class BankSellPage extends StatelessWidget {
  const BankSellPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Bank Sell Page",
    );
  }
}
